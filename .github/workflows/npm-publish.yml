name: Echo Test

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print message
        run: echo "Hello! GitHub Actions is working 🚀"
      - name: testing
        run: echo "hello"
